# Assigning values to variables
guest_count = 15
event_location = "Hitchin"
is_catered = True

# Updating "guest_count" value by 3
guest_count += 4
# 4 more guests confirmed
print(f"Updated guest count: {guest_count}")

# Updating with removal of 7 from guest_count
guest_count -= 7
print(f"Updated guest count is now: {guest_count}")